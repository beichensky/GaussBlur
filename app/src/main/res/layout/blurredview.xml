<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:tools="http://schemas.android.com/tools"
             tools:context="com.zgd.gaussblur.MainActivity"
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             android:orientation="vertical">

    <ImageView
        android:id="@+id/blurredview_blurred_img"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/blurredview_blurred_img_description"
        android:scaleType="fitXY"
        android:visibility="gone"/>

    <ImageView
        android:id="@+id/blurredview_origin_img"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/blurredview_origin_img_description"
        android:scaleType="fitXY"/>

</FrameLayout>